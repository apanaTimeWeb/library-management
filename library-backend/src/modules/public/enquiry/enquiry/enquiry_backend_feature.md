# Public Enquiry Module

This module handles incoming enquiries from the public frontend (e.g. `public/enquiry` route).

It strictly follows the micro-modularized Enterprise-grade architecture.

**Endpoints:**
- `POST /api/public/enquiry`: Creates a new enquiry in the system.
