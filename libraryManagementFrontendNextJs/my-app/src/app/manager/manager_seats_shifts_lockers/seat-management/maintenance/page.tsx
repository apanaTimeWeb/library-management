import { SeatMaintenanceClient } from '@/app/manager/manager_seats_shifts_lockers/manager_seats_shifts_lockers_components/SeatMaintenanceClient';

export default function SeatMaintenancePage() {
  return <SeatMaintenanceClient />;
}
