import { ShiftManagementClient } from '@/app/manager/manager_seats_shifts_lockers/manager_seats_shifts_lockers_components/ShiftManagementClient';

export default function ShiftManagementPage() {
  return <ShiftManagementClient />;
}
