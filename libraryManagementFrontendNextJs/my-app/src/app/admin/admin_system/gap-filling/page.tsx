import { AdminSystemGapFillingClient } from '@/app/admin/admin_system/admin_system_gap_filling_components/AdminSystemGapFillingClient';

export default function GapFillingPage() {
  return <AdminSystemGapFillingClient />;
}
